apiVersion: v1
kind: ConfigMap
metadata:
  name: kwite-config
data:
  url: "/mykwite"
  ready: "OK!"
  alive: "OK!"
  template: |
    this is a
    base template
    x is {{ .x }}
  ignoredkey: "this particular key will be ignored"
